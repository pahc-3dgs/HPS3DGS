# HPS3DGS 工作约定

本仓库用于维护 HPS3DGS、Geo33、Fig. 7、HAC 和 HAC++ 的现有可用路线。开始工作前阅读 `docs/VERSIONING.md` 和 `docs/VALIDATION_POLICY.md`，并核对当前目录、分支、主仓库 HEAD、子模块 HEAD 及未提交修改。

## 修改范围

1. 根据任务目标决定范围。更新数据、绘图、文字或修复局部问题时复用已有实现，保留原结构、接口、数据格式和操作方式，不顺带重构。开发、性能优化和算法探索可比较新方案，但原型与正式实现分开。
2. 已授权范围内自主推进，不重复索要确认；发现其他改进机会不等于获得扩大范围的授权。需要结构调整时说明原因与影响。
3. 不撤销他人修改。遇到相关文件已有改动时，先记录和核对归属；使用独立分支及工作树完成当前任务。不要对原研究目录或共享数据执行 `reset --hard`、`clean`、递归删除或覆盖。
4. 原 HPS3DGS 保持 `src/`、`scripts/` 等结构。依赖代码在各自子模块内修改并提交，再由主仓库固定其提交号；不将子模块文件复制成第二份活跃实现。
5. 保留各路线的模型、训练环境和码流边界。Fig. 7 的 Geo32 优化及五组 PTQ、Geo33 的四组 QAT及存储扩展、HPS3DGS、HAC、HAC++ 分别验证。HAC/HAC++ 的 `codec_only` 接入不代表模板归属传播或 owner-aware 联合训练。
6. 固定小表绘图沿用已有矩阵或指定飞书数据源，不主动改为实验 JSON 加载器；实际查看输出后再调整布局。

## 实施与验证

- `main` 只接收已达到相应验收要求的提交；日常使用 `fix/`、`feature/` 或 `repro/` 分支及隔离工作树。初次整合未完成验收时留在整合分支并标为候选版本。
- 先保留复现证据，再修复。为影响行为的 bug 添加能覆盖该故障的回归验证，运行与修改范围相称的检查；文档和布局小改不添加只复述实现的测试。
- 每次运行使用新的输出目录。记录实际命令、完整提交号、子模块状态、运行环境、输入/压缩包哈希、视图列表和结果。GPU 作业前检查占用，沿用明确选定的空闲设备，不停止其他用户作业。
- 区分“代码检查通过”“代表包独立解码/重建通过”“算法目标达成”。同一组 150 张图用于训练和重建时，写明 `150 train / 150 reconstruction views（同一组视图）`，不得称为 held-out 测试。
- 必需检查出现失败、缺失依赖、跳过、缺失参考值或不可比较协议时，不能按通过合入或发布。保留证据，报告具体阻塞；不补造成功记录。
- 完成前检查实际 diff、未跟踪文件及输出，只提交与当前目标有关的文件。提交主仓库前确认子模块没有未提交改动，且固定的提交有可恢复来源。
- 提供修改报告：修改文件与目的、复用内容、验证命令和真实结果、未解决限制、原目录/数据保护情况、主仓库及依赖提交号。可使用 `docs/templates/CHANGE_REPORT.md`。
- 不自动推送远端、不删除旧工作树或历史结果、不移动已发布标签。后续发布或清理按用户授权范围执行，不添加逐步确认流程。
